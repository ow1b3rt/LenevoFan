first run setup (install sdl + initial setup -> acpi is kernel level module, we need to set fan_control bit in modprobe)

```bash
chmod +x ./setup.sh
./setup.sh
```

build

```bash
make
```

run

```bash

make run
```
